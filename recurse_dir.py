#import os
#from typing import TextIO
#from utils import is_junction, uprint, now_str, Criteria

# Assuming the names in params are already converted to lowercase and
# that has_... bools have been set up in params object.
# EXIST: has_filenames, has_excl_filename, has_max_bytes, has_extensions, has_excl_ext

